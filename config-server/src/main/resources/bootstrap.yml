spring:
  application:
    name: configserver
  cloud:
    config:
      server:
        git:
          uri: file://var/projects/local/microservices-demo/config-server-repository
          clone-on-start: true
    fail-fast: true